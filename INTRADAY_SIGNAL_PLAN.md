# 盘中择时信号设计方案 v2

> 依据 `INTRADAY_SIGNAL_REVIEW.md` 评审意见修订（v1 → v2）。实施记录按惯例追加至 MODEL_CHANGES.md。

## 1. 背景与目标

**问题**：现有竞价预测回答"收盘 vs 开盘方向"，与你的实际决策（盘中"现在能不能买"）错位；盘中重跑还会混入已发生的行情，造成追高误导。

**目标**：基于分钟线构建盘中动量评分，回答"当前点位是否适合买入/持有/离场"。规则版先行（无需训练数据，立即可用），阈值经实测后在 v2 用积累数据标定。

**契约**：信号回答"当前点位动量是否配合买入/持有/离场"，是**未来 30–60 分钟方向倾向**（55–60% 命中为现实预期），是决策过滤器而非保证；震荡市信号会反复翻转，必须配合仓位与止损纪律。

## 2. 数据输入

| 输入 | 来源 | 备注 |
|------|------|------|
| 目标股分钟线 | `fetchIntraday`（ifzq minute/query，已存在 StockApi.kt:113） | 时间索引 0=9:30…240=15:00，午休压缩 |
| 指数分钟线 | 同接口，代码取 `SECTOR_MAP[code]?.indexCode ?: FALLBACK sz399001` | 实现时先验证 sz399006/399001 格式支持；不支持则降级为纯个股动量并标注 |
| 昨收 / 持仓 | UI 已有（acc.totalQty、prevClose） | — |

**取点原则（评审 ③）**：`parseMinuteIndex`（StockApi.kt:150）将 11:30 与 13:00 均映射为 index 120，分钟列表存在两个 minute=120 的点。**所有"最近 N 分钟"特征一律按列表位置取点，严禁按 minute 值定位**。

## 3. 特征定义（均从分钟线纯函数计算）

| 特征 | 公式 | 说明 |
|------|------|------|
| `mom15` | 按位置取点：price[last] vs price[last−15] 涨跌幅 % | 短动量，需 ≥16 点 |
| `mom30` | 按位置取点：price[last] vs price[last−30] 涨跌幅 % | 中动量，需 ≥31 点 |
| `rsIndex` | mom30(个股) − mom30(指数) | 相对强弱；指数缺失时=0 并标记降级 |
| `dayGain` | 现价 vs 昨收 累计涨幅 % | 当日累计涨幅，追高拦截依据 |
| `volRatio` | 最近5分钟均量 / 全天均量（**按位置差分**：perVol[i] = cumVol[i] − cumVol[i−1]） | 放量确认 |
| `aboveAvg` | 现价 vs 该分钟均价（`avgPrice`）%，单位：百分点 | 站上均价线=多头 |
| `fromHigh` | 现价 vs 日内最高 %（≤0） | 追高拦截依据 |

**时段过滤（评审 ③⑤）**：

| 时段 | 处理 |
|------|------|
| 开盘 30 分钟内（位置 < 40） | 不出信号 → "数据积累中" |
| 午后 13:00–13:30（位置 121~150） | 不出信号 → "数据积累中"（mom30 跨午休跳变无意义） |
| 午间休市（最后点位置 = 120 且下一段未开始） | 不出信号 → "午间休市" |
| 15:00 后 | 不出信号 → "已收盘" |

## 4. 评分与决策

```
score = 1.0×mom15 + 0.8×mom30 + 1.5×rsIndex + 2.0×aboveAvg + 0.8×volBoost
volBoost = +1（放量且 mom30>0）/ −1（放量且 mom30<0）/ 0（其他）
```

**量纲说明（评审 ①）**：aboveAvg 系数由 ×10 改为 ×2，单位明确为百分点；五项常态量级可比（mom15 ±1.0 / mom30 ±1.2 / rsIndex ±1.5 / aboveAvg ±1.0 / volBoost ±0.8），不再被均价线距离一家独大。

**无持仓**：

| 条件 | 结论 | 显示 |
|------|------|------|
| score ≥ +2.5 | `BUY` | 买入信号 |
| score ≤ −2.5 且 mom30 < 0 | `NO_TRADE` | "看跌，暂不建议买入"（评审 ④ 语义拆分） |
| 其他 | `WATCH` | 观望 |

**有持仓（评审 ⑥ 合并分支）**：

| 条件 | 结论 | 显示 |
|------|------|------|
| score ≤ −2.5 | `SELL` | 转弱，考虑减仓/离场 |
| 其他 | `HOLD` | 持有观察 |

**追高拦截（评审 ②，覆盖慢涨案例）**：命中 `BUY` 但 `(dayGain > 2.0% 或 mom30 > 1.5%)` 且 `fromHigh > −0.5%` → 降级 `WATCH`，原因标注"已接近日内高点，追高风险大"。

**数据不足**：分钟点 < 40 → `NO_TRADE`（"数据积累中"）。

## 5. 集成改动

| 文件 | 改动 |
|------|------|
| `IntradaySignal.kt`（新增） | 纯函数：特征计算（按位置取点）+ 评分 + 决策，无 Android 依赖，可单测 |
| `StockViewModel.kt` | 新增 `signal: StateFlow<IntradaySignal?>`；加载目标/指数分钟线（并发）→ 计算信号；**指数数据缓存 ~60 秒（评审 ⑧），个股分钟照旧 10 秒刷新** |
| `ui/StockApp.kt` | 分时弹窗（IntradayDialog）图表上方加信号横幅：等级 + 分数 + 原因列表；弹窗已有 10 秒自动刷新，信号随之跳动 |
| `IntradaySignalTest.kt`（新增） | 合成分钟序列单测（见 §7） |

## 6. 展示样例

```
[买入信号 5.2]  30分钟动量+0.8% · 相对指数+0.6% · 站上均价 · 放量
[警告] 已接近日内高点（距最高 −0.3%），追高风险大

[已收盘] / [午间休市] / [数据积累中] / [看跌，暂不建议买入]
```

## 7. 测试计划（12 例）

1. 分钟点 < 40 → NO_TRADE "数据积累中"
2. 开盘 30 分钟内 → NO_TRADE "数据积累中"
3. 午后 13:00–13:30（位置 121~150）→ NO_TRADE "数据积累中"
4. 单调上涨 + 站上均价 + 指数强 → BUY
5. 慢涨案例：dayGain > 2% 且近高点 → 降级 WATCH + 追高警告
6. mom30 > 1.5% 且近高点 → 降级 WATCH + 追高警告
7. 下跌 + 跌破均价 + 指数弱（无持仓）→ NO_TRADE "看跌，暂不建议买入"
8. 有持仓：弱势 → SELL；强势 → HOLD
9. 指数数据缺失 → 降级输出（rsIndex=0，原因注明）
10. 午休边界：列表含两个 minute=120 点，按位置取点不崩溃且特征正确（断言 perVol 差分与 mom30 取点）
11. 放量下跌 → 负 volBoost
12. 非交易时段（15:00 后 / 午休）→ 标注"已收盘 / 午间休市"

## 8. 实施状态（v3 预留 → v9 已落地）

- ✅ 信号快照落盘 + 30/60 分钟后结果回填 + 方向命中率/扣费期望统计（v9：`IntradaySignalStore` + `IntradaySignalWorker` + `statsOf`）
- ⏳ 用积累数据标定 BUY/SELL 阈值与权重（沿用 Calibration 的衰减加权思路）——待样本积累到可统计量级后实施
- 未做：信号变化通知推送（v3 预留，仍不在范围）

## 9. 风险

- 指数分钟接口对指数代码的格式支持需先实测（不支持则退化为纯个股动量并标注）
- 阈值是经验值，初期需观察调整
- 信号是方向倾向不是保证：必须配合仓位与止损纪律使用

---

*本方案为数据参考，不构成投资建议。*
