# 盘中择时信号评审（INTRADAY_SIGNAL_REVIEW）

> 独立评审文档，不包含在方案文档（`INTRADAY_SIGNAL_PLAN.md`）内。
> **约定：最新评审放在最前面（§1 为最新），旧评审依次后移。** 实施记录见 `MODEL_CHANGES.md`。

## 评审历史（最新在前）

| 顺序 | 评审 | 对象 | 结论 |
|------|------|------|------|
| 最新 | §1 v6 代码评审 | 盘中信号实施（IntradaySignal 等） | **可合入**；建议合入前加固"顶部反转盲区"，并补 AFTERNOON 分支测试 |
| ↓ | §2 v2 方案复审 | INTRADAY_SIGNAL_PLAN.md v2 | 全部评审点已落实，可以实施 |
| 最早 | §3 v1 方案评审 | INTRADAY_SIGNAL_PLAN.md v1 | 方向可行，需修 9 点 |

---

# §1 v6 代码评审（最新）

> 对应 `MODEL_CHANGES.md` v6。评审对象：`IntradaySignal.kt`（新增）、`IntradaySignalTest.kt`（新增）、`PredictionViewModel.kt`（重写）、`StockViewModel.kt`、`ui/StockApp.kt`、`SnapshotStore.kt`（去重修复）、`PredictionEngineTest.kt`。验证：全量 189 用例通过（含 IntradaySignalTest 12 例、去重回归 1 例）。

## 1.1 总评

- **代码正确性：高。** 无致命 bug；评分公式、追高拦截、时段语义与方案 v2 一致；按位置取点正确处理了 minute=120 重复索引；午后 121~150 排除区间正确避免了 mom30 跨午休。
- **数据去重修复：正确且必要。** `addSnapshot`/`addRecord` 按日期去重（SnapshotStore.kt:44/67），回归测试覆盖"3 次点击仅 1 条记录"，放量基线（volZ）不再被重复点击污染。
- **并发防护：合格。** `refresh/refreshAll` 用自增 token + `cancel()` 防乱序覆盖；`async/awaitAll` 并发拉取；指数 60 秒缓存已按 §3.3⑧ 落实。
- **模型可靠性：可用但存在一个实质性盲区**（见 1.2①），建议合入前加固。

## 1.2 模型可靠性问题

**① 顶部反转盲区（最重要的模型缺陷）**
- `aboveAvg` 用的是**当日 VWAP（滞后指标）**。快速拉升后价格从高点回落的最初几分钟内，价格仍明显高于陈旧的 VWAP，`aboveAvg` 保持大正值，`score` 可能仍 ≥ +2.5 → **在下跌刚开始时仍显示 BUY**。
- 数值验证：10.00 快速拉到 10.20（+2.0%）后见顶回落，回落头 3–5 分钟内 mom30 仍为正、aboveAvg≈+0.9%，score≈2.5 → 仍是 BUY。这恰恰是最容易追进去的时点。
- 且追高拦截只在 `fromHigh > −0.5%`（贴近日内高点）时生效；一旦价格已从高点回落 0.5% 以上，拦截解除，而 VWAP 仍读多头。
- **建议（v2.1）**：BUY 增加确认条件 `mom15 > 0`（短动量向上才买）；并对 `fromHigh ≤ −1.0%`（已明显回落）禁止出 BUY（降级 WATCH/NO_TRADE）。

**② 追高拦截边界盲区（已知，非 bug）**
- `dayGain > 2.0%` 为严格大于，恰好 +2.0% 不触发；慢涨到顶但 `dayGain ≤ 2%` 且 `mom30 ≤ 1.5%` 的温和追高仍会出 BUY。建议后续把阈值做成参数，便于实测微调。

**③ 非 A 股个股的信号语义错配（低）**
- 分时弹窗对所有股票开放（未按 `isPredictable` 过滤）。港股/美股若 minute 接口返回数据，会用兜底指数 sz399001（深证成指）做 `rsIndex`，基准错配。建议：非 A 股不计算信号，或使用对应指数（hkHSI 等）并标注。

**④ 可靠性可视性退化（产品取舍，提醒）**
- 竞价预测详情弹窗（含分目标前推回测命中率）已下线，用户在 App 内不再能看到模型实测命中率，无法自证"可靠与否"。若在意透明度，可考虑在关于页或设置中保留竞价回测统计入口。

## 1.3 实现缺陷与细节

| # | 问题 | 严重度 | 说明 |
|---|------|--------|------|
| ⑤ | `CLOSED` 依赖 241 个点 | 低 | 若 minute 接口少返回一行，15:00 后可能判为 AFTERNOON 出陈旧信号；建议叠加墙钟判定（评审 §2.2b 原建议） |
| ⑥ | 指数接口实机未验证 | 待验证 | `minute/query` 对 sz399006/399001 的返回格式需真机确认；失败已走降级路径（degraded + 标注），风险可控 |
| ⑦ | `refreshAll` 多股共享同指数代码时可能并发重复拉取 | 无害 | 缓存写入是幂等的，仅冗余请求，不影响正确性 |
| ⑧ | `StockViewModel.loadIntraday` 串行拉指数再拉个股 | 轻微 | 每 10 秒两次串行请求，信号略滞后；可改 `async` 并发 |
| ⑨ | 死代码 | 轻微 | `StockApp.kt:635 signedPct()` 已无引用（详情弹窗删除后遗留），建议清理 |
| ⑩ | `dayGain == 2.0%` 恰好不触发拦截 | 边界 | 严格大于的边界抖动，改为 `>=` 或参数化即可 |

## 1.4 测试覆盖缺口

- **⑪ AFTERNOON 信号分支从未被测试**：`IntradaySignalTest.kt` 用例 10 第二部分列表长度实为 151（lastPos=150），落在 AFTERNOON_START（≤150）而非 AFTERNOON（≥151）；用例 4–8 全为 MORNING（lastPos≤44）。**盘中信号最常出现的午后 13:30+ 分支（真正产出 BUY/HOLD/SELL 的路径）没有用例**。建议补一条：完整上午 121 点 + 午后 ≥31 点，断言 AFTERNOON 下 BUY/HOLD 生效。
- **⑫ 无"顶部反转"用例**：没有"冲高回落后 score 仍 ≥2.5 残留 BUY"的断言，无法暴露 1.2① 的盲区。若实施 1.2① 加固，应补对应回归用例。

## 1.5 结论与建议

- **可合入**：代码正确、测试绿、去重修复有价值。1.3 除⑧⑨外均可接受。
- **建议加固（合入前或 v2.1）**：1.2① 顶部反转盲区（mom15>0 确认 + 回落禁 BUY）是当前模型最该补的一刀，直接关系到"买在顶部"这一类亏损。
- **建议补齐**：1.4⑪ AFTERNOON 分支测试；⑫ 顶部反转回归用例。
- **顺带清理**：⑨ signedPct 死代码；⑥ 真机验证指数分钟接口。

---

# §2 v2 方案复审（对应 INTRADAY_SIGNAL_PLAN.md v2）

**结论：全部评审点已落实，可以实施。** 剩余为实施细节建议，不阻塞。

## 2.1 评审点落实情况

| 评审点 | v2 处理 | 状态 |
|--------|---------|------|
| ① 量纲失衡 | aboveAvg ×10→×2，单位明确百分点，量纲说明补入 §4 | ✅ |
| ② 追高拦截 | 新增 `dayGain`，条件改 `(dayGain>2% 或 mom30>1.5%) 且 fromHigh>−0.5%` | ✅ |
| ③ 午休边界 | 新增"取点原则"、按位置取点、午后 121~150 过滤；测试补断言（用例 10） | ✅ |
| ④ NO_TRADE 语义 | 拆分"数据积累中"/"看跌，暂不建议买入" | ✅ |
| ⑤ 非交易时段 | 午间休市/已收盘标注（§3 时段过滤） | ✅ |
| ⑥ HOLD 死分支 | 有持仓分支合并为 SELL/HOLD | ✅ |
| ⑦ volRatio | 明确按位置差分 perVol[i]=cumVol[i]−cumVol[i−1] | ✅ |
| ⑧ 指数缓存 | §5 明确 ~60 秒缓存 | ✅ |
| ⑨ 指数接口 | 保留实测 + 降级路径 | ✅ |

## 2.2 实施细节建议（不阻塞，实施时注意）

- **a. `rsIndex` 对齐**：指数与个股分钟列表点数可能不一致（并发请求时序差），`mom30(指数)` 应按指数序列**各自的尾部相对取点**（idx[last−30] vs idx[last]），不要用与个股相同的绝对位置。
- **b. 时段判定主用墙钟**：午间休市与 13:00 最后点都是 minute=120，无法仅靠数据区分；应以 `System.currentTimeMillis` 判定交易时段，数据位置仅作辅助。避免"最后点=120"歧义。
- **c. `dayGain` 空安全**：新加股票可能无 `prevClose` → `dayGain=null`，追高拦截退化为只看 `mom30>1.5%`（OR 语义天然兼容），实现需允许 null。
- **d. `volRatio` 前置**：点数 <6 时无法算 5 分钟差分 → `volBoost=0`（因信号仅位置 ≥40 才出，实际不影响，但实现要防御）。
- **e. 数据新鲜度（可选）**：网络慢时分钟数据可能滞后 1–2 分钟，横幅可加"更新于 HH:mm"提示，避免拿旧数据当"现在"决策。

## 2.3 结论

方案 v2 已覆盖 v1 评审全部要点，工程可行、可单测，具备实施条件。建议按 §2.2 a–d 落实，e 可选。

---

# §3 v1 方案评审（对应 INTRADAY_SIGNAL_PLAN.md v1）

## 3.1 总评

**方向正确、工程上可行。** 核心风险不在"能不能做"，而在**信号质量与实现边界**。

| 维度 | 结论 |
|------|------|
| 可行性 | 高：纯函数 + 无 Android 依赖 + 可单测 + 复用现有分钟线，随时能跑 |
| 数据 | 目标股分钟线已就绪（`fetchIntraday`，StockApi.kt:113）；`avgPrice` 已由累计额/累计量算好（Model.kt:196），无需额外字段 |
| 指数 | sz399006/399001 代码存在（MarketData.kt:157/165），接口格式需运行时实测 |
| 主要风险 | 评分公式量纲失衡、追高拦截覆盖不足、午休边界脏数据 |

## 3.2 必须修正的缺陷

**① 公式量纲失衡：`aboveAvg×10` 一家独大（最高优先级）**

```
score = 1.0×mom15 + 0.8×mom30 + 1.5×rsIndex + 1.2×aboveAvg×10 + 0.8×volBoost
```

- `aboveAvg` 单位是百分点（如 +0.3 = 站上均价 0.3%），×10 即 ±3；而 mom30 常态仅 0.3~1.5%（×0.8 ≤ 1.2）。score 几乎被"现价相对均价线"支配。
- 后果：上涨趋势中价格几乎总在均价上方 → **BUY 常亮**；价格上下穿均价线 → 信号频繁翻转（震荡市被打脸）。
- 建议：`aboveAvg` 改用 tanh 或更大除数（如 ×2 而非 ×10），并**明确单位（小数 or 百分点）**，让五项量级可比。

**② 追高拦截未必能挡住"159915 教训"**

- 原条件 = `mom30 > 1.5% 且 fromHigh > −0.5%`。实测案例是"慢涨到午间最高点"：mom30 未必超 1.5%，但当日累计涨幅已很大。
- 建议：拦截改为 `dayGain > 2%` **或** `mom30 > 1.5%`（新增 `dayGain` 因子 = 现价 vs 昨收累计涨幅），且 `fromHigh > −0.5%`，任一命中即降级 WATCH。

**③ 午休边界会产生脏数据（实现陷阱）**

- `parseMinuteIndex`（StockApi.kt:150）把 11:30 与 13:00 都映射为 index 120，分钟列表存在**两个 minute=120 的点**（241 个点、index 120 重复）。
- 所有"最近 15/30 分钟"特征必须**按列表位置取点**，严禁按 minute 值定位。
- 午后 13:00–13:30 的 mom30 会跨午休（11:30→13:00 价格跳变），无意义：建议位置 121~150 同样不出信号（与开盘同理）。

## 3.3 建议一并修正

| # | 问题 | 建议 |
|---|------|------|
| ④ | NO_TRADE 语义重复：`数据不足` 与 `看跌别买` 共用标签 | 分开显示："数据积累中" / "看跌，暂不建议买入" |
| ⑤ | 非交易时段出陈旧信号：15:00 后 / 午休时仍会算 BUY | 仅交易时段（9:30–15:00，午休除外）出信号，否则标注"已收盘/午间休市" |
| ⑥ | 有持仓分支死代码：`−2.5<score<+1.0 → HOLD` 与 `≥+1.0 → HOLD` 均为 HOLD | +1.0 分界无效，合并或删除 |
| ⑦ | `volRatio` 的"全天均量"计算 | 按列表位置（点数+1）差分，不能用 minute 值 |
| ⑧ | 每 10 秒多一次指数分钟请求 | 指数数据缓存 ~60 秒，个股分钟仍 10 秒刷新 |
| ⑨ | 指数分钟接口对指数代码的格式支持未知 | 运行时验证 sz399006/399001；不支持则降级为纯个股动量（标注） |

## 3.4 确认保留的设计

- 规则版先行（无需训练数据），v2 再落盘+回填+标定——路线正确。
- 追高拦截思路正确，但需按 §3.2② 加强。
- 10 条单测计划覆盖了午休边界（用例 9），需按 §3.2③ 补一条"按位置取点"断言。

## 3.5 决策契约

- 信号回答"当前点位动量是否配合买入/持有/离场"，是**方向倾向（55–60% 命中）而非保证**。
- 即便实现完美，震荡市仍会反复翻转；必须配合仓位与止损纪律。

---

*本评审为数据参考，不构成投资建议。*
